query surveys($limit: Int, $offset: Int) {
    surveys(limit: $limit, offset: $offset) {
        _id
        name {
            en
        }
        responses {
            _id
        }
        createdAt
    }
}
